rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /images/{relationshipId}/{weekId}/{userId}_{imageType}.jpg {
      allow read: if request.auth != null;
      allow write: if request.auth.uid == userId;
    }
  }
}
